const initialState = {
    firstName: "",
    lastName: "",
    email: "",
    password: "",
    confirmPassword: ""
}

export const reducer = (state = initialState, {type, payload})=>{

    switch (type) {
        case type:
            
            break;
    
        default:
            break;
    }
}